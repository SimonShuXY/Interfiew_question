# 贡献指南

感谢你改进这本 Agent 手册。为了保持内容连续、可验证、适合 0 基础读者，请遵守以下约定。

## 内容原则

- 每章都要说明与前一章、后一章的关系。
- 新增框架、协议、模型能力时，尽量引用官方文档或论文。
- 避免只堆术语，优先解释工程判断：什么时候用、什么时候不用、失败模式是什么。
- 示例代码优先保持最小依赖，除非该章节明确讨论某个框架。
- 涉及安全、隐私、医疗、法律、金融等高风险场景时，必须写明限制和人工审核要求。

## 提交流程

1. 新建分支。
2. 修改或新增文档。
3. 本地运行 `mkdocs build --strict` 检查文档站点。
4. 提交 PR，并说明改动范围、资料来源和是否影响章节顺序。

## 章节命名

章节文件使用两位数字排序，例如：

```text
docs/04-tools-function-calling.md
```

如果插入新章节，请同步更新 `README.md`、`SUMMARY.md` 和 `mkdocs.yml`。
